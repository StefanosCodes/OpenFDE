packages:
  - studio

allowBuilds:
  # These optional transitive packages are not needed for the starter eval or
  # results dashboard. Keep their install scripts disabled by default.
  '@playwright/browser-chromium': false
  '@swc/core': false
  esbuild: false
  onnxruntime-node: false
  protobufjs: false
  sharp: false
